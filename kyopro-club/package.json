{
    "scripts": {
        "build": "npm run build:backend",
        "build:backend": "cd ./backend && npm run build",
        "build:watch": "concurrently \"npm run build:backend:watch\"",
        "build:backend:watch": "cd ./backend && npm run build:watch",
        "start": "node backend/dist/index.js"
    }
}
